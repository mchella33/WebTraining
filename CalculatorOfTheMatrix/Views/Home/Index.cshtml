@model CalculatorOfTheMatrix.Models.RolColViewModel
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@using (Html.BeginForm("InputGrid", "Home"))
{
    @Html.TextBoxFor(m => m.Columns, new { @type="number", @min="0"})
    @Html.TextBoxFor(m => m.Rows, new { @type="number", @min="0"})

    <div>
        <input type ="submit" value="Submit"/>
    </div>
}