@model MatrixCalculatorHW.Models.Matrix

@{
    ViewBag.Title = "Results";
}

<h2>Results</h2>

@for (int i = 0; i < Model.Mesh.Length; i++)
{
    for (int m = 0; m < Model.Mesh[i].Length; m++)
    {
        @Model.Mesh[i][m];
    }
    <br/>
}