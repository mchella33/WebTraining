@model CalculateTheMatrix.Models.Matrix
@{
    ViewBag.Title = "Answer";
}

<h2>Answer</h2>
<div class="container">
    <div class="row">
        <div class="col-sm-12">
            @for (int i = 0; i < Model.Grid.Length; i++)
            {
                    for (int x = 0; x < Model.Grid[i].Length; x++)
                    {

                    @Model.Grid[i][x]
                    @:&nbsp;

                    }
                    <br />
               

            }
        </div>

    </div>
</div>